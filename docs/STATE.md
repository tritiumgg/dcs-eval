# Working state

**Last updated:** 2026-09-19

The handoff between sessions. Read it first; update it before a session ends,
not only when a task finishes. Stamp the date above each time; it carries a
date and nothing else, because what changed is what the sections below are for.

**This file is loaded cold every session, so its size is a tax on all of them.**
Each section has a line budget and `tools/statecheck.sh` enforces it. Over
budget, nothing is deleted — it moves. A completion older than the last few
goes to git log. A choice with reasoning behind it becomes a decision record.
A durable fact about the project belongs in `CLAUDE.md`. A resolved
carry-forward is just deleted. One or two lines per entry, never paragraphs.

---

## In progress

Nothing. T55 landed; T56 has not started.

*One task at most. Say what is done, what is not, and where to resume. Say what
is committed and what is only in the working tree. Say what is knowingly
broken. Empty this when the task closes.*

## Just finished

- **T55** — the file-source reader, the other half of `evalFile`: `cargo test -p dcs-eval file_source` prints 39 and the crate 274. `source::read` takes T34's `Admitted` and **takes no headers, and derives no name**: it puts the body behind the very block `check` framed and reads the `chunkname` off that same block — `check` lifts the value out of the slice it framed and keeps it beside the bytes — so "the reader cannot disagree with what is being sent" is structural and there is no second set to disagree, which is why no `HeaderDrift` mutation exists to run. A request naming no chunk records `=dcs-api-eval`, the far end's own default, rather than a path nothing carries. The order is name, open, read, size, mark, shebang, empty, hash: the 200-byte cap falls on the header value going out because the framer caps no value at any length, the size is compared against the raw bytes before any rule shortens them, the mark comes off *before* the `#` line is looked for, and the digest is over the body that is sent and not the file on disk (ADR 0015, which also holds the two thresholds and the backslash spelling the record's example line does not use). SHA-256 is written in the crate per ADR 0011 and proved twice: the three published vectors, and `sha256sum` on a fixture at every length 0..=130 and over 300 kB — `sha256sum` marks a line whose name holds a backslash by putting one in front of the digest, which every Windows path here earns, so the helper strips it. **What Lua prints is 52 bytes, not 60**: measured on the pinned 5.1.5 at name lengths 50 to 90, a raise shows the whole path at 52 and `...` plus the last 52 from 53 up, a compile error the whole path at 72 and `...` plus the last 72 from 73 up — 60 is `LUA_IDSIZE`, the buffer, and `luaO_chunkid` spends eight of it before it copies. Both frozen documents print 60 and both are describing the buffer; an assertion written against it would have been wrong in both directions. So `chunkid` is pinned against the interpreter at seven lengths and the line-47 control computes its expectation through it, with two guards in front — the path is past the threshold, and the message begins `...` — so a fixture that stopped exercising the case fails rather than passes. Eleven mutations were seen red. The shebang dropped rather than blanked reddens four, the line-47 test among them with `line47.lua:46:` against `:47:`, which is the whole reason the fixture counts to 47; a `\r\n` converted on the way in reddens the byte-for-byte control with a `13` missing in front of every `10`, and the line-47 test stays green on 47 because Lua counts both alike — which is why the bytes are the control and the line number is not; the hash taken over the bytes as read reddens the two-readings test alone; `Bom::None` although one was stripped reddens four; the mark stripped *after* the `#` test reddens three, the line-47 file then failing to compile at all on `unexpected symbol near '#'`; the `Empty` refusal dropped reddens three, each handing back a `Source` of zero bytes; the 200-byte cap dropped reddens the header-value test; the name derived from the path rather than read off the block reddens all three name tests; the eight-byte reserve cut to zero reddens four including the line-47 test; and the `Grew` check dropped reddens one, while the same check moved past the two rules reddens the zero-headroom fixture that grows by a mark alone — the raw-bytes comparison is ADR 0015 reading 7. **One mutation did not redden**: `FILE_FLAG_OPEN_REPARSE_POINT` taken off the open leaves the swapped-leaf control green, because a junction followed leads to a directory and opening a directory as a file is refused anyway — so the flag's own contribution is unproven on this machine and the test says so in its comment. The `Relinked` arm is unproven with it: a file symlink here needs elevation (`mklink`, observed unelevated: "You do not have sufficient privilege to perform this operation."), and the swapped-leaf test asserts the refusal it really saw, `Open` with "Access is denied." An agent verified every claim on this machine under `mise exec -- cargo test`, restoring each mutation from a saved copy and `cmp`ing it back. **It did not see DCS.** The raise on line 47 is the reference interpreter's under a driver reading the body in `"rb"`, not a game's; the two thresholds were measured on Lua 5.1.5 PUC-Rio on this box and not inside a game process; the ceiling comes off a stand-in's handshake; and nothing here has been *sent* — MCP §7's "one at the ceiling is sent whole" is proved as far as *framed to exactly `max_request_bytes`*, no further.
- **T34** — what `evalFile` refuses before a byte is read: `cargo test -p dcs-eval file_refusals` prints 29, and the crate 235. This is half of what was one row; the reader that hashes the bytes is T55, and the split is at the point where a byte is first read. `Roots::judge` makes no filesystem call at all and answers three rules in one order — a `Config` per write directory supplied, since `DCS*` is a glob and the variants sit side by side under one allowed `Saved Games`, then the install, then the allowed roots — so a user whose allowed root covers `Config` is told the real reason and not the weaker one; `check` then frames the caller's own headers, stats the file, and refuses on `size + header_block > max_request_bytes`. The one stat answers twice, and the second answer is a refusal of its own: a directory passes containment and stats at no length, so the ceiling has nothing to catch it with, and admitting it would hand the reader a path it can only fail to open — it is refused in its own words, as is anything that is neither a regular file nor a directory, which has no length a stat settles and no end a read reaches. That guard deleted reddens the directory test alone, with `Admitted { size: 0, headroom: 262122 }` where a refusal was wanted. `Admitted` now records a second gap beside the stat-to-read one: a leaf that did not exist when the path was resolved had nothing of its own to follow, so a link put there afterwards is one this judgement never saw and T55 closes it by judging the handle. ADR 0014 settles the four things the frozen text left a library unable to do: the caller supplies the roots and an empty allow list admits nothing rather than falling back to wherever this process is sitting, the install and write directory are supplied and never taken from the handshake's reported `install_guard`, "at the ceiling" is the *file's* ceiling so the comparison is the sum and not the limit less the header block, and `check` takes a `paths::Real` because only the resolver makes one — the parameter type *is* the proof that resolution preceded judgement, which no comment could be. The before-reading control is the whole of the row and it is a fixture, not an argument: three files held open with `share_mode(0)`, which denies this process's own reads, driven through the whole of `check`. Ten mutations were seen red. The read moved to the top of `check` reddens six (the three held fixtures, the no-content sweep and the two framer tests, whose fixtures are absent files); moved into `judge` instead it reddens nine, adding the held outside-every-root fixture, the existence check and the directory test — and the plain containment tests stay green under both, because their fixtures are ordinary readable files, which is what makes the held ones the control rather than decoration. What that control proves is narrower than "no byte is touched": the same `std::fs::read` at the top of `judge` with its `Result` discarded rather than propagated leaves all 29 green, since nothing then depends on what the read said, so the fixtures hold "no refusal is decided by a read" and a reader that opened and threw the bytes away would pass. The ceiling block deleted reddens five and leaves `admits_a_file_exactly_at_the_file_ceiling` green, which is why that test cannot be the control; `262_144` in place of the handshake's figure reddens the three tests whose session says something else and leaves the three on the default green; the header block dropped from the sum reddens four including the exactly-at-the-ceiling test's headroom; and the sum rewritten as `size > max - header` reddens **one alone** — the empty file under a header block past the limit, the case that spelling admits although its framed request is already over. The install branch dropped reddens four and both `Config` tests stay green; the `Config` branch dropped reddens three and every install test stays green; the guard narrowed to the first write directory reddens the two-variant test alone, which is why that test is the one holding the glob; `Config` excused by an allowed root reddens both `Config` tests; the roots rule moved in front of the other two reddens six, each naming the roots rule where a stronger one was wanted; and a refusal made to append "(the path exists)" reddens three, which is the only assertion holding "a refusal never says whether a path outside the roots exists". `resolve` short-circuited past `canonicalize` reddens both install-spelling tests here *and* six of T30's own `paths` suite, so it is not an isolated mutation and cannot be made one: `Real`'s field is private, so no unresolved `Real` can be fabricated to bypass the check, and that impossibility is the finding. An agent verified every claim itself on this machine under `mise exec -- cargo test`, restoring each mutation from a saved copy and `cmp`ing it back. It did not see DCS, and it did not need one: every refusal is decided before the disk is touched, and the ceiling comes off a stand-in's handshake. Two things it did **not** observe. Whether the held fixture behaves the same elevated is unknown — every run here was unelevated, and `share_mode(0)` is a sharing decision rather than an access-control one, so it is expected to hold either way and has not been watched. And nothing here is *sent*: MCP §7's "one at the ceiling is sent whole" is proved only as far as **admitted**, the send being T55's. The seam ADR 0014 leaves open is not a carry-forward because the record holds it, which is what that section says an eleventh entry means: a caller that supplies no write directory gets no credential guard and one that names a single variant gets it for that variant, and `dcs-mcp serve` refusing to serve without a write directory, and finding the `DCS*` siblings of the one it was given, are Stage 7's.
- **T33** — the client's `pipeline(specs, W)` and the id nothing minted before: `cargo test -p dcs-eval pipeline` prints 17, `id` 7, and the crate 206. An id is `<seq>-<tag>`: the counter is zero-padded to ten digits, which is what makes "lowest id" and "published first" the same question the executor's sorted listing answers, and the tag is eight base36 characters fixed for one minter's life, seeded per ADR 0011 from the process id, the clock, a stack address and a process-local counter. The counter is the honest part: it is what makes two minters in one process distinct by mechanism, which is exactly and only what the 1,000-minter test claims — nothing here says anything about two *processes* started in the same millisecond, and the test says so in its own comment. The encoder reduces modulo 36^8 and left-pads, because a naive base36 of a small draw is under `is_id`'s four characters and of a huge one over its twelve; both ends are pinned on the encoder directly rather than on one lucky draw. The window is W *published* requests, refilled before the yield and never after, and the head of the line is waited on by name however long ago anything behind it landed. ADR 0013 decides the five answers the frozen text leaves open: a timed-out head yields `pending` and frees its slot, a spec the framer refuses waits in the queue and is yielded in its own place without costing the window a request, that queue bounded at twice the depth so a run of refusals cannot mint an id for a whole spec list in one call, a terminal head stops publication and each id still in flight is collected once before it is reported *in the head's own terminal word* rather than always `superseded`, a `wait` that cannot read the session ends the drain once and for ever and hands its window back by name through `in_flight()` rather than draining it, since a published request runs whether or not the client can read the answer and the same unreadable session would fail every one of them in turn, and a counter with no ten-digit `seq` left is yielded *behind* the window it had already published rather than in front of it, once, ending the drain — in front of it the requests on the disk would never be waited on and their ids would never reach the caller, and without the ending `for outcome in pipeline` would never return. The ordering control is the one that matters and it forces disorder on the disk rather than by sleeping: a new `Standin::tick_with` lets the fixture wait for all four requests to be published, answer 4, 3 and 2, then wait for three replies plus 100 ms — four of the client's polls — before answering 1, and the test asserts both that the yields ascend and that the fixture really did answer backwards, so it can never pass quietly. The window is proved from the session's side, never from the client's bookkeeping: nine specs three deep give the census `[3,3,3,3,3,3,3,2,1]`, which a batch of three cannot produce (it gives `[3,2,1,3,2,1,3,2,1]`), and a second reading on the other side of W catches a flood. Eighteen of the twenty-one mutations were seen red — a true arrival-order head reddens the backwards control (`left: [02,03,04,01] right: ascending`) and leaves the in-order baseline and both window checks green, which is why the baseline cannot be the control; batching reddens the census with `[3,2,1,3,2,1,3,2,1]` and flooding with `saw 9 requests in req/, wanted at most 3`; a tag drawn per id reddens the one-tag check with 50 tags; an unpadded seq reddens the sort across the power-of-ten boundary; the mixer's last xor-shift dropped reddens the golden tag; the seq guard dropped reddens the eleven-digit refusal; an encoder neither reduced nor padded reddens `draw 0 encoded as 0` at one character and the golden tag at thirteen; the pick ignored reddens both stand-in checks *and* the backwards control's second assertion, which is how that assertion earns its place; publication past a terminal outcome reddens six requests where three were wanted; a neighbour reported without its one `collect` reddens the reply that landed before the kill; a held slot behind a `pending` reddens with `[01,01,01,01]` against the four ids; a refused send ending the drain reddens with two items where five were wanted; a `WaitError` kept in flight reddens "the drain ends there"; an unbounded fill reddens "the first call minted an id for every spec in the list" against five unframeable specs two deep; a neighbour whose `collect` failed dropped from the window reddens "both neighbours are still nameable"; an exhausted counter reported in front of the window reddens with the refusal alone where the id it had published was wanted first; and the same refusal yielded without ending the drain reddens with four copies of it against two items. Two were argued and not run, and the argument is in the comment and in ADR 0013: refilling after the yield rather than before reddens only intermittently, and a neighbour given `superseded` regardless of the head's kind has no fixture, because the crate cannot produce a `dead` head with a window in flight without inventing a whole test for a one-line branch. One was run and did **not** redden: dropping the process-local counter from the seed left the 1,000-minter check green on this box three times over, the clock separating adjacent calls by itself, so the counter's contribution is unproven here rather than proved. An agent verified every claim itself on this machine under `mise exec -- cargo test`, restoring each mutation from a saved copy and `cmp`ing it back. It did not see DCS: every reply came from the stand-in, which answers every request it lists by construction, so "answers until the tick budget is spent and the rest waits for the next tick" — the thing the window exists to exploit — is still unobserved, and `Minter` has no owner yet. `README.md` needed no change; it carries no library-API prose.

*The last three at most, one line each. Git log holds the rest.*

## Next

**Task T56** — the event-driven reply watch: `ReadDirectoryChangesW` and the
five symbols beside it, declared in `sys.rs` under ADR 0011, with T54's 25 ms
poll kept as the fallback §3.2 requires for a watch that reports nothing.
`CancelIoEx` before the overlapped buffer goes out of scope is not optional.
Done when `cargo test -p dcs-eval watch` shows a reply woken on rather than
polled for, a silent watch still answered, and no handle held. Needs T54.

**An agent verifies** it here: every event is this test's own fixture, so
nothing sees DCS. Whether the watch reports on the filesystem a user's
`Saved Games` sits on is Stage 9's.

## After that

- **Stages 0 to 2 are closed, and Milestone A with them:** the wire proven off
  DCS, the stand-in, the interop and round-trip controls. T17 closed it.
- **Stage 3 is closed:** eval across the carriers with `<file>:47` true in
  every state, and the `a_do_script` shift reproduced.
- **Stage 4's path is closed and Stage 5 has opened:** `tick-budget: 524`, `instr-budget: 2209`, `fence: 583`, `events: 114`, `dormant: 40`, `arming: 95`, `heartbeat: 300`. Stage 6 has opened on the client side: `paths: 15`, `readers: 24`, `wait: 32`, `status: 24`, `id: 7`, `pipeline: 17`, `file_refusals: 27`, `file_source: 39` under `cargo test`, the crate at 274.
  Milestone B needs T53, the rest of Stages 5 and 6, the mutation sweep.
- **Stage 9** is the critical path and cannot be shortened by parallel effort.
  Everything provable off DCS is proved before it.

## Carries forward

Things that must not be lost between sessions. Delete an entry when it is
resolved, and say where. Mark an entry only the maintainer can settle. Ten
entries at most: an eleventh means something here is finished, or belongs in
`docs/decisions/` or `CLAUDE.md` instead.

- **Cutover — the incumbent goes before this executor installs.** `dcs-api-bridge`'s
  `DcsApiEval.lua` sits in the same `Scripts\Hooks\`; ADR 0002 is why the names
  differ; T52 proves two hooks on one root cannot happen.
- **Maintainer decision — when the MCP registration is swapped.** Claude Code still
  points at `dcs-api-bridge`; the swap strands a session mid-task, so it happens at
  Milestone C. ADR 0001.
- **What only Stage 9 sees.** The load sentinel covers two: the hook guard's
  swallow path, which has no seam until a raising stub sits on the frame path, and
  whether the export state survives between missions. The wrapper is proved under
  the suite's own carrier alone — what DCS does with one raising inside
  `net.dostring_in`, and whether every state has `setfenv` and `_G`, is unmeasured,
  the model's stubs evaluating nothing — and the carrier against the incumbent's
  measured shift and `...`, not T49's. Unmeasured too: DCS's `os.clock` and
  `os.time` against the harness's, a count hook raising inside either carrier or
  already held by a state (`none`, ADR 0005), what `dcs.log` renders around a
  crossing's markers, which the reader ignores (ADR 0007), and what
  `lfs.tempdir()` really gives, which every fixture supplies by hand.
- **Declared before served, and absent before counted.** The handshake
  publishes `ops`, `states`, `eval` and the five figures from the first load,
  the two instruction figures provisional. No `state` is `bad-request`: the maintainer's call, 2026-09-11.
- **A path with a byte past ASCII stops the load.** Header values are ASCII, so a user name past ASCII puts one in every path the handshake names and the load refuses, naming the header in `dcs.log` (`docs/audit.md`, Open: the spec says nothing).
  The client parses it as the executor does, the maintainer's call at T13; a
  spelling for such a path on the wire is the writer's side, unsettled.
- **The disarm owes a sweep, and no row asks for one.** Its heartbeat half is
  built (T29); the sweep on that same disarm is unclaimed.
- **The held sibling is a held file.** The sweep's "cannot be removed" path is proved
  with a file the suite keeps open; the client's own handle is T56's, unheld at T41.
- **The round trip's `superseded` half is unblocked and unclaimed.** T17's row
  parks it on T54, which landed the fence against the stand-in and does not ask
  for it; no row claims it against the shipped Lua.
- **A `Minter` has no owner.** The window mints from whatever it was handed; who
  holds one across tool calls, so two do not restart at seq 1, is Stage 7's.
  `Minter::seeded_at` resumes a counter.
