[package]
name = "dcs-eval"
description = "The client half of dcs-eval: speaks the executor's file-based protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[features]
# The stand-in executor, a test double that plays the executor's side of the
# wire in-process. The crate's own tests always compile it; a crate that
# drives it from its tests enables this in its dev-dependency on dcs-eval.
standin = []
